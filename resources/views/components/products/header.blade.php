@props(['title', 'description'])

<div class="text-center mb-12">
    <h2 class="text-3xl font-bold text-gray-900 mb-4">{{ $title }}</h2>
    <p class="text-gray-600 max-w-xl mx-auto">{{ $description }}</p>
</div>